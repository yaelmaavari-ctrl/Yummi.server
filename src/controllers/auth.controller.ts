/**
 * Auth controller. Owner: Developer A.
 *
 * TODO: implement handlers (register, login, switchActiveRole, getMe).
 * Wrap async handlers with `asyncHandler` (../utils/asyncHandler), throw
 * `ApiError` (../utils/ApiError) on failure, delegate logic to authService,
 * and wire them in routes/auth.routes.ts.
 */
export const authController = {};
