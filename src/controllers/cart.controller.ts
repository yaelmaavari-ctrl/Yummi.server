/**
 * Cart controller. Owner: Developer B.
 *
 * TODO: implement handlers (getCart, addItem, updateItem, removeItem, clear).
 * Delegate logic to cartService and wire in routes/cart.routes.ts.
 */
export const cartController = {};
