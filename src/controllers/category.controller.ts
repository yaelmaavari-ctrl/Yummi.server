/**
 * Category controller. Owner: Developer A.
 *
 * TODO: implement handlers (list, getById, create, update, softDelete).
 * Delegate logic to categoryService and wire them in routes/category.routes.ts.
 */
export const categoryController = {};
