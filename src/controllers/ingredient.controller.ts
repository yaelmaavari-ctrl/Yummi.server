/**
 * Ingredient controller. Owner: Developer A.
 *
 * TODO: implement handlers (list, create, update, updateStock, setStatus).
 * Emit INGREDIENT_AVAILABILITY_CHANGED via sockets when status changes.
 * Delegate logic to ingredientService and wire in routes/ingredient.routes.ts.
 */
export const ingredientController = {};
