/**
 * Notification controller. Owner: Developer B.
 *
 * TODO: implement handlers (listMine, markAsRead, markAllRead).
 * Delegate logic to notificationService and wire in routes/notification.routes.ts.
 */
export const notificationController = {};
