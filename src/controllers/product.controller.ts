/**
 * Product controller. Owner: Developer A.
 *
 * TODO: implement handlers (list, getById, create, update, setAvailability,
 * softDelete). Emit PRODUCT_AVAILABILITY_CHANGED via sockets when availability
 * changes. Delegate logic to productService and wire in routes/product.routes.ts.
 */
export const productController = {};
