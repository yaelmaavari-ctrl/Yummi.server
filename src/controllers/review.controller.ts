/**
 * Review controller. Owner: Developer A.
 *
 * TODO: implement handlers (create - one per COMPLETED order, immutable;
 * listByProduct). Delegate logic to reviewService and wire in
 * routes/review.routes.ts.
 */
export const reviewController = {};
