/**
 * Auth service - business logic. Owner: Developer A.
 *
 * TODO: implement register (hash password with bcryptjs), login (verify +
 * issue JWT via ../utils/jwt), switchActiveRole (re-issue token with a role
 * the user owns), getMe. Reads/writes the User model.
 */
export const authService = {};
