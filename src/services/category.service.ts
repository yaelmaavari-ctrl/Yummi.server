/**
 * Category service - business logic. Owner: Developer A.
 *
 * TODO: implement list/getById/create/update/softDelete. Block soft delete
 * when active (non-deleted) products still reference the category.
 * Reads/writes the Category model.
 */
export const categoryService = {};
