/**
 * Notification service - business logic. Owner: Developer B.
 *
 * TODO: implement create + listMine + markAsRead. Used to notify customers when
 * an order becomes READY and admins when a kitchen issue is reported (pair the
 * persisted notification with a real-time socket emit). Reads/writes the
 * Notification model.
 */
export const notificationService = {};
