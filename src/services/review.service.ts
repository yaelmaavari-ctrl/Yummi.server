/**
 * Review service - business logic. Owner: Developer A.
 *
 * TODO: implement create (only for the customer's own COMPLETED order, exactly
 * one per order, immutable) and listByProduct. Reads/writes the Review model.
 */
export const reviewService = {};
