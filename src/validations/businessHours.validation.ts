import Joi from 'joi';

/**
 * Business hours validation schemas (Joi). Owner: Developer A.
 *
 * TODO: define updateScheduleSchema / specialDaySchema rules.
 */
export const updateScheduleSchema = Joi.object({});
export const specialDaySchema = Joi.object({});
