import Joi from 'joi';

/**
 * Category validation schemas (Joi). Owner: Developer A.
 *
 * TODO: define createCategorySchema / updateCategorySchema rules.
 */
export const createCategorySchema = Joi.object({});
export const updateCategorySchema = Joi.object({});
