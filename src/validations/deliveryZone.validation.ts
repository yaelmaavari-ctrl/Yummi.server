import Joi from 'joi';

/**
 * Delivery zone validation schemas (Joi). Owner: Developer A.
 *
 * TODO: define create/update rules (city, deliveryPrice, estimatedDeliveryMinutes).
 */
export const createDeliveryZoneSchema = Joi.object({});
export const updateDeliveryZoneSchema = Joi.object({});
