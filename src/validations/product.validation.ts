import Joi from 'joi';

/**
 * Product validation schemas (Joi). Owner: Developer A.
 *
 * TODO: define createProductSchema / updateProductSchema / setAvailabilitySchema rules
 * (name, price, category, ingredients, allowedExtras, freeExtrasCount, pricePerExtra).
 */
export const createProductSchema = Joi.object({});
export const updateProductSchema = Joi.object({});
