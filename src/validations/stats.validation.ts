import Joi from 'joi';

/**
 * Statistics validation schemas (Joi). Owner: Developer B.
 *
 * TODO: define query schemas (e.g. date-range filters for revenue) if needed.
 */
export const statsQuerySchema = Joi.object({});
